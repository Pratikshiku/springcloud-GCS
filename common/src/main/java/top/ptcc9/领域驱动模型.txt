


vo: 视图对象，用于展示层。作用是把某个指定页面（或组件）的所有数据封装起来。
dto: 数据传输对象，泛指用于展示层与服务层之间的数据传输对象。
do: 领域对象，即从现实世界中抽象出来的有形或无形的业务实体。
po: 持久化对象，与数据库一一映射。


前后端分离请求过程：
1. 用户发出请求（可能是填表单），表单数据在展示层被匹配为vo。
2. 展示层把vo转换为服务层对应方法要求的dto，传递给控制层。
3. 服务层首先根据dto的数据构造（或重建）一个do，调用do的业务层方法完成具体业务。
4. 业务层把do转化为持久层对应的po，调用持久层方法完成持久化。